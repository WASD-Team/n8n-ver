# 📦 Содержимое deploy пакета

## Структура файлов

```
deploy/
├── 📄 docker-compose.yml         # Основной файл Docker Compose
├── 📄 .env.example               # Шаблон переменных окружения
├── 📄 .gitignore                 # Исключения для git
│
├── 📚 Документация
│   ├── README.md                 # Полное руководство
│   ├── QUICKSTART.md             # Быстрый старт
│   ├── ARCHITECTURE.md           # Схема архитектуры
│   └── CHANGELOG.md              # История изменений
│
├── 🚀 Автоматизация
│   ├── setup.sh                  # Автоматическая установка (Linux/macOS)
│   ├── setup.ps1                 # Автоматическая установка (Windows)
│   └── Makefile                  # Управление через make
│
└── 🗄️ init/                      # SQL скрипты инициализации БД
    ├── 01-create-databases.sql
    ├── 02-app-schema.sql
    ├── 03-versions-schema.sql
    ├── 04-seed-versions.sql
    ├── 04-settings-schema.sql
    ├── 05-instances-schema.sql
    └── 06-invite-tokens-schema.sql
```

## Что делает каждый файл

### docker-compose.yml
Описывает два сервиса:
- **postgres** - PostgreSQL 16 с автоинициализацией
- **web** - Веб-приложение из GHCR

### .env.example
Шаблон конфигурации со всеми переменными:
- Настройки PostgreSQL
- ENCRYPTION_KEY для шифрования
- Интеграция с n8n
- Порты сервисов

### setup.sh / setup.ps1
Скрипты автоматической установки:
1. Проверяют Docker
2. Создают .env из шаблона
3. Генерируют случайный ENCRYPTION_KEY
4. Логинятся в GHCR
5. Запускают контейнеры
6. Проверяют готовность

### Makefile
Удобные команды:
- `make setup` - автоустановка
- `make up` - запустить
- `make down` - остановить
- `make logs` - логи
- `make update` - обновить
- `make backup` - бэкап БД
- и многое другое...

### init/*.sql
SQL скрипты для автоматической инициализации БД:
- Создание баз данных и пользователей
- Схемы таблиц
- Индексы
- Начальные данные

## 🎯 Варианты использования

### 1️⃣ Автоматическая установка (самый простой)
```bash
cd deploy
./setup.sh  # Linux/macOS
.\setup.ps1 # Windows
```

### 2️⃣ С использованием Make
```bash
cd deploy
make setup
make logs
make backup
```

### 3️⃣ Ручной запуск
```bash
cd deploy
cp .env.example .env
nano .env  # отредактируй переменные
docker compose up -d
```

## ✨ Ключевые возможности

✅ **Запуск одной командой** - `docker compose up -d`
✅ **Автоматическая инициализация БД** - все схемы создаются автоматически
✅ **Healthchecks** - контроль здоровья сервисов
✅ **Персистентность данных** - volume для PostgreSQL
✅ **Безопасность** - пароли в .env, автогенерация ключей
✅ **Обновления** - `docker compose pull && docker compose up -d`
✅ **Бэкапы** - встроенные команды для резервного копирования
✅ **Логирование** - удобный просмотр логов

## 🔧 Требования

- Docker 20.10+
- Docker Compose v2
- 512MB RAM
- 1GB места на диске
- GitHub Personal Access Token для GHCR

## 📖 Дальнейшие шаги

1. Прочитай [QUICKSTART.md](QUICKSTART.md) для быстрого старта
2. Изучи [README.md](README.md) для полного понимания
3. Посмотри [ARCHITECTURE.md](ARCHITECTURE.md) для понимания структуры
4. Запусти проект одной командой!
