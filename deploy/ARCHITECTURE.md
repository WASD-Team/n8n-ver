# 🏗️ Архитектура деплоя

```
┌─────────────────────────────────────────────────────┐
│                     Docker Host                      │
│                                                      │
│  ┌──────────────────┐      ┌───────────────────┐   │
│  │   Web Container  │      │ PostgreSQL 16     │   │
│  │                  │      │                   │   │
│  │  n8n-ver-web     │─────▶│  • app_db        │   │
│  │  (Next.js)       │      │  • versions_db   │   │
│  │                  │      │                   │   │
│  │  Port: 3000      │      │  Port: 5432      │   │
│  └──────────────────┘      └───────────────────┘   │
│         │                          │                │
│         │                          │                │
│         └──────────┬───────────────┘                │
│                    │                                │
│              ┌─────▼─────┐                         │
│              │  Volumes  │                         │
│              │           │                         │
│              │ postgres_ │                         │
│              │   data    │                         │
│              └───────────┘                         │
└─────────────────────────────────────────────────────┘
                    │
                    │ Port 3000
                    ▼
            ┌───────────────┐
            │   Nginx/      │
            │   Traefik     │
            │  (optional)   │
            └───────────────┘
                    │
                    │ HTTPS
                    ▼
            ┌───────────────┐
            │    Users      │
            └───────────────┘
```

## Компоненты

### Web Container
- **Образ:** `ghcr.io/altvk88/n8n-ver-web:latest`
- **Функции:** 
  - Next.js веб-приложение
  - API endpoints
  - UI для управления версиями
- **Healthcheck:** `/api/debug`

### PostgreSQL Container
- **Образ:** `postgres:16-alpine`
- **Базы данных:**
  - `app_db` - пользователи, настройки, аудит
  - `versions_db` - версии workflow
- **Автоинициализация:** SQL скрипты из `/init`

### Volumes
- `postgres_data` - персистентное хранилище БД

## Поток данных

```
User → Web UI → Next.js API Routes → PostgreSQL (app_db)
                                   → PostgreSQL (versions_db)
                                   → n8n Webhook (restore notifications)
```

## Безопасность

- Все пароли в `.env`
- `ENCRYPTION_KEY` для шифрования паролей БД
- Сетевая изоляция через Docker networks
- Healthchecks для мониторинга
